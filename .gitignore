node_modules/
dist/
.env
*.log
.next/
.vercel/
# `pnpm deploy` output (see zerops.yaml) — build artifact, regenerated on every Zerops build
out/
# Generated product code (agent-coder output) — regenerated by running the swarm, not source
products/
# Claude Code local tool state — not project source
.claude/
# Nested submission-repo working copy (its own separate git repo) — never nest it in this one
/swarmforge/
